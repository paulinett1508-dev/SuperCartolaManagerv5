# Super Cartola Manager

## Scripts

- `start`: Inicia o servidor com `node index.js`.
- `dev`: Inicia o servidor em modo de desenvolvimento com `nodemon index.js`.
- `test`: Executa testes (requer Jest).
- `predeploy`: Incrementa automaticamente a versão do projeto (ex.: 1.0.0 -> 1.0.1) antes de cada deploy, usando `npm version patch --no-git-tag-version`.