🎯 TAREFA: PADRONIZAÇÃO DE MÓDULOS DO APP PARTICIPANTE

📋 CONTEXTO:
O app do participante (/public/participante/) tem um header FIXO já definido no index.html. 
Porém, vários módulos JS estão renderizando seus próprios headers internos, causando inconsistência visual.

🏗️ ARQUITETURA CORRETA:
- index.html → Header fixo (já existe, não alterar)
- index.html → Container de módulos (#boas-vindas-container ou similar)
- index.html → Bottom nav fixo (já existe, não alterar)
- Módulos JS → Devem renderizar APENAS conteúdo, SEM headers próprios

📁 ARQUIVOS A AUDITAR:
Pasta: /public/participante/js/modules/
Verificar TODOS os arquivos participante-*.js:
- participante-extrato.js
- participante-extrato-ui.js
- participante-ranking.js
- participante-rodadas.js
- participante-top10.js
- participante-mata-mata.js
- participante-pontos-corridos.js
- (e qualquer outro módulo existente)

🔍 O QUE IDENTIFICAR E REMOVER:
Procurar por padrões de "header interno" como:
- Divs com classes: bg-gradient-to-br, from-orange-*, from-zinc-*
- Headers com ícones + título do módulo (ex: "EXTRATO", "RANKING")
- Estruturas tipo: <div class="...header..."><h2>NOME DO MÓDULO</h2></div>
- Qualquer elemento que pareça um cabeçalho de seção principal

✅ MÓDULO DE REFERÊNCIA (PADRÃO CORRETO):
Arquivo: /public/participante/js/modules/participante-boas-vindas.js
Este módulo NÃO renderiza header próprio - começa direto com conteúdo.
Usar como exemplo do padrão correto.

🎨 DESIGN SYSTEM A SEGUIR:
- Fundo de cards: bg-surface-dark (ou #1c1c1c)
- Texto secundário: text-white/70, text-white/50 (não usar text-gray-*)
- Cor primária: text-primary (ou #FF4500) - não usar text-orange-*
- Bordas: border-white/10 (não usar border-zinc-*)
- Cards: rounded-xl (não usar rounded-lg em cards principais)

📝 AÇÃO PARA CADA MÓDULO:
1. Abrir o arquivo
2. Localizar a função que renderiza o container.innerHTML ou similar
3. Identificar se existe um "header interno" no início do template
4. REMOVER o header interno, mantendo apenas o conteúdo
5. Ajustar classes CSS para o design system (bg-surface-dark, text-white/*, etc)
6. Manter toda a lógica de negócio intacta

⚠️ IMPORTANTE - NÃO ALTERAR:
- Lógica de cálculos e processamento de dados
- Chamadas de API
- Event listeners e interações
- Estrutura de funções exportadas
- Apenas o VISUAL/HTML renderizado

🔄 EXEMPLO DE TRANSFORMAÇÃO:

ANTES (ERRADO):
```javascript
container.innerHTML = `
    <!-- Header do Módulo -->
    <div class="bg-gradient-to-br from-orange-900/40 to-orange-900/10 p-3 rounded-lg border border-orange-800/50 mb-4">
        <div class="flex items-center space-x-2">
            <span class="material-icons text-orange-400">icon</span>
            <h2 class="text-xs font-bold text-orange-400 uppercase">NOME MÓDULO</h2>
        </div>
    </div>
    <!-- Conteúdo real -->
    <div class="card">...</div>
`;
```

DEPOIS (CORRETO):
```javascript
container.innerHTML = `
    <!-- Conteúdo direto, sem header -->
    <div class="bg-surface-dark rounded-xl p-4 mb-4">...</div>
`;
```

✅ CHECKLIST FINAL:
- [ ] Todos os módulos auditados
- [ ] Headers internos removidos
- [ ] Classes CSS padronizadas (bg-surface-dark, text-white/*, text-primary)
- [ ] Nenhuma lógica de negócio alterada
- [ ] App testado e funcionando

🚀 EXECUTE A TAREFA